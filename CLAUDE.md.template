# 工程紀律模板（挑你認同的併進你的 CLAUDE.md）

> 這是從一套真實運作的工作紀律抽出的**通用骨架**，零專案私有內容。
> 不要照單全收——紀律要你自己認同、你的專案真的需要，才放進去。每條都附「為什麼」。

## 核心十條

**1. 元層優先**：遇到 bug，先問「這是個案，還是某個 pattern 的一次發作？」。同類錯誤出現 ≥3 次 = 系統性問題，去建一個機制（hook/test/lint）擋它，而不是第 4 次再手修。
> 為什麼：手修個案是線性成本，建機制是一次性投資擋掉未來所有同類。

**2. 靠機制不靠承諾**：「我下次會記得 X」不算數。把 X 寫進 hook、test、CI、或 CLAUDE.md。
> 為什麼：你（和模型）一定會忘。能持久的只有自動觸發的機制。

**3. commit 前真實驗證（Verify Before Commit）**：commit 前跑 test + 真實 runtime smoke，不是「讀起來對」。改了會被載入的 runtime code，commit 後要真的重啟 + 看 log 確認生效。
> 為什麼：「import 成功」≠「runtime 正確」。靜態看對的東西在真實執行時常常壞。

**4. 一 commit 一件事（Atomic）**：A/B/C 三件事拆三個 commit。
> 為什麼：bug 出在 C 時，能乾淨 rollback C 而不牽連 A/B。

**5. 看真實再下結論**：描述「系統怎麼運作」「某模組做什麼」前，先 `ls` 檔案、`grep` caller、跑一次。禁止憑記憶或憑「設計上應該」回答。
> 為什麼：記憶會過期，設計意圖 ≠ 運行事實。憑印象描述系統是幻覺的頭號來源。

**6. 自報數字必驗**：任何「有 N 個」「已經 X」「應該是 Y」，下結論前先 grep/ls/count 確認真實值。
> 為什麼：對「具體可驗證數字」的記憶/推測錯誤率很高。先驗一次省掉一連串基於錯誤前提的工作。

**7. 串行防 race**：多個 agent/process 動同一個 git 工作樹時，串行執行，不並行。
> 為什麼：並行 `git add` 會互相吃掉對方 staged 的內容，commit 變得張冠李戴。

**8. 誠實優先於好看**：metric/分數掉了就說掉了，去找真實原因（log），不為了數字好看而隱瞞或粉飾。
> 為什麼：虛假撐起的高分比真實的低分更危險——前者讓你基於假象做決策。

**9. 減法優先於加法**：要建新模組/script/hook 前，先 `ls`/`grep` 盤點現有的，問「能不能擴既有的？」。
> 為什麼：重複造輪子是最常見的浪費。多數「我需要一個新 X」其實是「我沒找到已有的 X」。

**10. 不確定就標記，不要硬幹**：搬不動的、不確定會不會壞的、需要對方拍板的——標記出來回報，不要猜著做。
> 為什麼：基於錯誤假設往前衝，是協作中最貴的失敗。

## 用法建議

- 不是每條都適合每個專案。小專案可能只需要 3/4/5/6。
- 把你選的條目，連同「為什麼」，貼進你專案 `CLAUDE.md`。「為什麼」很重要——模型（和未來的你）需要理解理由才會真的遵守。
- 搭配 `hooks/` 裡的 hook，這些紀律會在對應時機被自動提醒，而不只是躺在文件裡。
